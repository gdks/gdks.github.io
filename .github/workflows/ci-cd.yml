name: Continuous Deployment (CI Checks Disabled - Use ci.yml)

# This workflow is for deployment only - CI checks are handled by ci.yml
on:
  push:
    branches: [main]
  schedule:
    # Run security scans weekly
    - cron: '0 0 * * 0'

env:
  NODE_VERSION: '18'

jobs:
  # ============================================================================
  # DEPLOYMENT JOBS ONLY - CI checks moved to ci.yml
  # ============================================================================

  # Deployment job (placeholder)
  deploy:
    runs-on: ubuntu-latest
    needs: [] # No dependencies since CI is handled separately
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deployment Placeholder
        run: |
          echo "This workflow is for deployment only."
          echo "All CI checks are now handled by ci.yml workflow."
          echo "Add your deployment steps here."
