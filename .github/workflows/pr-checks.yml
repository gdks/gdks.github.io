name: PR Checks (DISABLED - Use ci.yml instead)

# This workflow is disabled to avoid conflicts with ci.yml
# All PR checks are now handled by the main CI workflow
on:
  # Disabled - no triggers
  # pull_request:
  #   branches: [ main, develop ]
  # pull_request_target:
  #   branches: [ main, develop ]

env:
  NODE_VERSION: '18'

jobs:
  # This workflow is disabled
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow is disabled. All checks are now handled by ci.yml"
          echo "Please use the main CI workflow for all PR and branch checks."
